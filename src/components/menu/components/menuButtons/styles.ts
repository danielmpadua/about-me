import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
	container: {
		zIndex: 10,
	},

	itemContainer: {
		display: "flex",
		alignItems: "center",
		justifyContent: "center",
	},
});
